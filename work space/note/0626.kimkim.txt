

import export할떄.

import할떄도 권한이 필요하다는걸 알았음.
그전에는 import하기전에 

할당과 동시에 선언을 하는방법은 var varchar2(20) ;= hello wordl  <-- 예시


오라클 같다는 =   ,   %는type정할떄말해주는것

만들 변수명  in 테이블명.기존변수명%type <-- 만들 변수명을 기존변수명의 데이터타입으로 정함.


create or replace로 테이블하나만들고 is begin으로 프로시저형태로만들면

exec형식으로 출력을하면 프로시저가 호출이되고 seelct*from 테이블명 형식으로 호출하면 테이블내용만 호출이됨.

선언을 해주는것이여서 위의 방식으로는 바로 출력은 이뤄지지않음/.